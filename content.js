// chrome.runtime.onMessage.addListener(function(request, sender, sendResponse) {
//     if (request.action === "injectCookies") {
//       // Handle the message and inject cookies
//       console.log("Received message to inject cookies:", request.cookies);
      
//         // Your code to inject cookies in the content script
//         if (request.action === "injectCookies") {
//             console.log(chrome.cookies);
//         }
//     }
//   });